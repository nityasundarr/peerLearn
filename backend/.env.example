# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# JWT
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Email (SMTP or transactional provider)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@peerlearn.edu.sg
FRONTEND_URL=http://localhost:5173

# OneMap API
ONEMAP_API_KEY=

# Google Maps API (internal distance computation only — never expose to frontend)
GOOGLE_MAPS_API_KEY=

# Fee schedule (SGD per hour per academic level)
FEE_PRIMARY=10
FEE_SECONDARY=12
FEE_JUNIOR_COLLEGE=15
FEE_POLYTECHNIC=15
FEE_ITE=12
FEE_UNIVERSITY=18

# Session coordination timeouts (hours)
TUTOR_RESPONSE_WINDOW_HOURS=48
TUTEE_CONFIRM_WINDOW_HOURS=24

# Penalty appeal window (days)
APPEAL_WINDOW_DAYS=7

# Rate limiting (max requests per hour per email address)
RATE_LIMIT_EMAIL_MAX=3
